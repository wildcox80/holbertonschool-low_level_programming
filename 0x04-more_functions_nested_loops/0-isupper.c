#include "holberton.h"

/**
 * _isupper - checks for uppercase character.
 * @c : int type uppercase.
 * Return: A is : 1 or a is : 0.
 */
int _isupper(int c)
{
if
(c > 64 && c < 91)
return (1);

return (0);
}
