#include "holberton.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a : int type number.
 * @b : int type number.
 * Return: 100352 -1646592.
 */
int mul(int a, int b)
{
return (a * b);
}
