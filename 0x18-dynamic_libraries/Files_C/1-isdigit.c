#include "holberton.h"
#include <stdio.h>

/**
 * _isdigit - checks for uppercase character.
 * @c : int type digit.
 * Return: 0 is : 1 or not 0 is : 0.
 */
int _isdigit(int c)
{
if
(c > 47 && c < 58)
return (1);

return (0);
}
